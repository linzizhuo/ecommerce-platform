# Web程序设计（J2EE）课程设计 实践报告

**课程名称：** Web程序设计（J2EE）课程设计  
**项目名称：** 基于 Vue3 + Spring Boot 的高并发电商系统 CloudMall  
**专业班级：** 软工12301  
**指导教师：** 余华云  
**完成日期：** 2026年6月22日  

---

## 目录

1. [开发环境搭建及MV架构实现](#1)
2. [MVC架构设计与实现](#2)
3. [基于SpringBoot+Vue的网上购物系统的设计与实现](#3)
4. [前、后台网页及程序设计](#4)
5. [代码实现](#5)
6. [结束语](#6)
7. [软件运行配置](#7)
附录：源代码及数据库

---

## 1. 开发环境搭建及MV架构实现

### 1.1 IDEA开发环境搭建

**开发环境配置：**

| 工具 | 版本 | 说明 |
|------|------|------|
| JDK | 17.0.19 | Java开发工具包 |
| Maven | 3.8.7 | 项目构建管理 |
| IntelliJ IDEA | 2024.x | 集成开发环境 |
| MySQL | 8.0 | 关系型数据库 |
| Redis | 8.4.0 | 缓存与分布式锁 |
| Node.js | 18.19.1 | 前端运行环境 |
| Vite | 5.4 | 前端构建工具 |
| Git | 2.x | 版本控制 |

**环境搭建步骤：**
1. 安装 JDK 17 并配置 JAVA_HOME 环境变量
2. 安装 Maven 并配置 settings.xml
3. 安装 MySQL 8.0，创建数据库 ecommerce
4. 安装 Redis，配置 requirepass 密码
5. 安装 Node.js 及 npm
6. 使用 IDEA 创建 Spring Boot 项目

### 1.2 MV架构实现

MV（Model-View）架构是两层架构模式，Model 负责数据访问，View 负责页面渲染。本阶段使用 **SpringBoot + Thymeleaf** 技术栈实现。

**MV架构特点：**
- Controller 直接调用 Mapper 进行数据库操作
- 没有独立的 Service 业务逻辑层
- 页面由 Thymeleaf 模板引擎在服务端渲染
- 前端页面与后端代码耦合在同一工程中

**项目核心代码结构：**
```
src/main/java/com/cloudmall/
├── controller/          # 控制器（直接调Mapper）
│   ├── ProductController.java
│   └── UserController.java
├── mapper/              # 数据访问层
│   ├── ProductMapper.java
│   └── UserMapper.java
├── entity/              # 实体类
│   ├── Product.java
│   ├── Sku.java
│   └── User.java
└── CloudMallApplication.java

src/main/resources/
├── templates/           # Thymeleaf模板
│   ├── product/
│   │   ├── list.html    # 商品列表页
│   │   └── detail.html  # 商品详情页
│   └── user/
│       ├── login.html   # 登录页
│       └── register.html # 注册页
└── application.yml
```

**MV架构请求流程：**
```
浏览器 → Controller → Mapper → MySQL
                ↓
         Thymeleaf模板渲染
                ↓
         HTML响应给浏览器
```

**本阶段实现功能：**
- 商品列表展示（模板循环渲染）
- 商品详情查看（含SKU规格表格）
- 分类导航与搜索
- 用户注册与登录（Session保持登录态）

---

## 2. MVC架构设计与实现

### 2.1 MVC架构搭建

在MV架构基础上，增加 **Service 业务逻辑层**，形成标准的三层 MVC 架构。

**MVC三层对比MV两层：**

| 对比维度 | MV架构 | MVC架构 |
|---------|--------|---------|
| 分层数量 | 2层 | 3层（+Service） |
| Controller职责 | 直接调Mapper，含业务逻辑 | 仅参数校验+路由转发 |
| 业务逻辑位置 | 散落在Controller | 集中在Service层 |
| 事务管理 | 无 | @Transactional |
| 返回格式 | ModelAndView | 统一R\<T\> JSON |
| 异常处理 | 默认白页 | @RestControllerAdvice全局处理 |
| 密码存储 | 明文 | BCrypt加密 |
| 认证方式 | Session | JWT无状态Token |

**MVC架构请求流程：**
```
浏览器 → Controller → Service → Mapper → MySQL
                ↓          ↓
         参数校验    业务逻辑+事务
                ↓
         统一R<T> JSON返回
```

**Controller层核心代码示例：**
```java
@RestController
@RequestMapping("/api/user")
public class UserController {
    @Resource
    private UserService userService;

    @PostMapping("/login")
    public R<String> login(@RequestBody Map<String, Object> body) {
        return userService.login(
            (String) body.get("phone"),
            (String) body.get("password")
        );
    }
}
```

### 2.2 注册功能模块实现

**注册流程：**
1. Controller 接收 JSON 请求体 {phone, password, nickname}
2. Service 层进行业务校验（手机号唯一性、密码长度≥6）
3. BCrypt 加密密码：`BCrypt.hashpw(password, BCrypt.gensalt())`
4. Mapper 写入 MySQL 数据库
5. 返回 JWT Token 实现注册即登录

**核心代码（Service层）：**
```java
public R<String> register(String phone, String password, String nickname) {
    User exist = userMapper.selectByPhone(phone);
    if (exist != null) throw new BusinessException("手机号已注册");
    if (password.length() < 6) throw new BusinessException("密码长度不能少于6位");

    User user = new User();
    user.setPhone(phone);
    user.setPassword(BCrypt.hashpw(password, BCrypt.gensalt()));
    user.setNickname(nickname);
    userMapper.insert(user);

    String token = JwtUtil.generate(user.getId(), user.getPhone());
    return R.ok(token);
}
```

### 2.3 登录模块实现

**登录流程：**
1. Controller 接收 phone + password
2. Service 根据手机号查询用户
3. BCrypt 校验密码：`BCrypt.checkpw(password, user.getPassword())`
4. 签发 JWT Token（有效期24小时）
5. 前端存储 Token 到 localStorage，后续请求通过 Authorization Header 携带

**JWT认证拦截器：**
```java
@Component
public class JwtInterceptor implements HandlerInterceptor {
    @Override
    public boolean preHandle(HttpServletRequest request, ...) {
        String token = request.getHeader("Authorization");
        if (token != null && token.startsWith("Bearer ")) {
            token = token.substring(7);
        }
        if (token == null || !JwtUtil.validate(token)) {
            response.setStatus(401);
            return false;
        }
        request.setAttribute("userId", JwtUtil.getUserId(token));
        return true;
    }
}
```

---

## 3. 基于SpringBoot+Vue的网上购物系统的设计与实现

### 3.1 业务需求分析

**系统定位：** B2B2C 多商户电商平台，支持用户前台购物、商家后台管理、平台运营管理。

**用户角色：**

| 角色 | 描述 | 核心功能 |
|------|------|---------|
| 普通用户(C端) | 平台消费者 | 浏览商品、搜索、购物车、下单支付、订单管理、评价、领券 |
| 商家(B端) | 入驻商户 | 商品管理(发布/SKU/上下架)、订单发货、优惠券管理、数据看板 |
| 平台管理员 | 运营方 | 用户管理、商品审核、数据大屏、角色权限(RBAC) |

**核心业务流程：**
```
用户注册/登录 → 浏览商品(分类/搜索) → 商品详情(SKU选择) 
→ 加入购物车 → 领优惠券 → 结算下单 → 模拟支付 
→ 订单确认 → 商家发货 → 确认收货 → 评价商品
```

### 3.2 数据库设计

#### 3.2.1 E-R图

**核心实体关系：**
```
User 1──N Address         用户有多个收货地址
User 1──N Order            用户创建多个订单
User 1──N Review           用户发表多条评价
User 1──N UserCoupon       用户领取多张优惠券
Category 1──N Product      类目包含多个商品
Product 1──N SKU           商品有多个规格
Product 1──N Review        商品收到多条评价
Order 1──N OrderItem       订单包含多个商品明细
Order 1──1 Payment         订单对应一条支付记录
Merchant 1──N Product      商家发布多个商品
Merchant 1──N CouponTemplate 商家创建多个优惠券模板
CouponTemplate 1──N UserCoupon 优惠券模板被多个用户领取
```

**数据库划分（为微服务拆分做准备）：**

| 数据库 | 所属服务 | 核心表 |
|--------|---------|--------|
| db_user | user-service | t_user, t_user_address, t_merchant, t_admin_user, t_role, t_permission 等 |
| db_product | product-service | t_category, t_product, t_sku, t_sku_stock, t_product_image, t_review |
| db_order | order-service | t_order, t_order_item, t_payment, t_after_sale, t_coupon_template, t_user_coupon, t_seckill_session |

#### 3.2.2 数据字典

**t_user（用户表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键自增 |
| phone | VARCHAR(20) UNIQUE | 手机号 |
| password | VARCHAR(200) | BCrypt加密密码 |
| nickname | VARCHAR(50) | 昵称 |
| avatar | VARCHAR(500) | 头像URL |
| status | INT | 0禁用 1正常 |
| create_time | DATETIME | 注册时间 |

**t_product（商品SPU表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键自增 |
| merchant_id | BIGINT | 商家ID |
| category_id | BIGINT | 类目ID |
| name | VARCHAR(200) | 商品名称 |
| description | TEXT | 商品描述 |
| brand | VARCHAR(100) | 品牌 |
| status | INT | 0草稿/1待审/2已上架/3已下架 |
| main_image | VARCHAR(500) | 主图URL |
| create_time | DATETIME | 创建时间 |

**t_sku（SKU表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键自增 |
| product_id | BIGINT | 所属商品ID |
| spec_info | VARCHAR(200) | 规格JSON {"颜色":"黑","容量":"256GB"} |
| price | INT | 价格(分) |
| original_price | INT | 原价(分) |
| stock | INT | 库存数量 |

**t_order（订单表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键自增 |
| order_no | VARCHAR(30) UNIQUE | 订单号 |
| user_id | BIGINT | 用户ID |
| total_amount | INT | 总金额(分) |
| discount_amount | INT | 优惠金额(分) |
| pay_amount | INT | 实付金额(分) |
| status | INT | 0待付/1已付/2已发/3已完成/4取消 |
| coupon_id | BIGINT | 使用的优惠券ID |
| address_snapshot | VARCHAR(1000) | 收货地址快照JSON |
| create_time | DATETIME | 创建时间 |

**t_coupon_template（优惠券模板表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键自增 |
| name | VARCHAR(100) | 优惠券名称 |
| type | INT | 1满减券 2折扣券 |
| threshold | INT | 使用门槛(分) |
| value | INT | 满减券=减多少分 / 折扣券=折扣率(85=8.5折) |
| total_count | INT | 发行总量 |
| received_count | INT | 已领取数量 |

**t_review（评价表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键自增 |
| user_id | BIGINT | 用户ID |
| product_id | BIGINT | 商品ID |
| order_id | BIGINT | 订单ID |
| rating | INT | 评分1-5 |
| content | TEXT | 评价内容 |
| create_time | DATETIME | 评价时间 |

---

## 4. 前、后台网页及程序设计

### 4.1 导航设计

系统采用三端独立导航结构：

**C端（用户前台）：** `/` 为入口
- 顶部导航栏：CloudMall Logo(红色) + 搜索框 + 购物车图标 + 订单/用户菜单
- 分类导航栏：全部 + 一级类目 + 二级类目按钮组
- 商品卡片网格展示

**商家后台：** `/merchant/` 为入口
- 顶部导航：商家后台Logo + 看板/商品/订单/优惠券/回前台
- 左侧无侧栏，顶部导航切换

**平台管理后台：** `/admin/` 为入口
- 左侧深色侧边栏：数据大屏/用户管理/商家管理/商品审核/角色权限
- 右侧主内容区

### 4.2 首页设计

首页采用现代化电商设计风格：

- **搜索栏：** 居中大搜索框，支持关键词搜索
- **分类导航：** 圆角按钮组，一级类目（手机数码、电脑办公...）+ 二级类目展开
- **商品卡片：** CSS Grid 自适应布局（4/3/2列），每张卡片包含：
  - 渐变色占位图 + 首字母
  - 商品名称 + 品牌·分类标签
  - 价格 + 已售数量
- **热卖标签：** 前3个商品显示红色"热卖"角标
- **登录态感知：** 未登录显示登录/注册按钮，已登录显示用户下拉菜单

### 4.3 前台功能设计

**商品详情页：**
- 左侧渐变背景大图 + 右侧信息区
- SKU规格按钮组，点击切换
- 实时显示选中SKU的价格、库存
- 数量选择器 + 加入购物车/立即购买按钮
- 评价星级展示 + 评价列表（含评分、文字、时间）
- 购物车小车图标入口

**购物车：**
- Redis Hash 存储，Key: cart:{userId}，Field: skuId
- 支持修改数量、删除、勾选
- 合计金额实时计算
- 去结算跳转

**结算页：**
- 收货地址选择（Radio 列表）
- 商品清单确认
- 优惠券选择：显示已领券（满减券/折扣券），自动验门槛，折扣自动计算
- 支付方式选择（支付宝/微信模拟）
- 实付金额展示 + 提交订单

**订单管理：**
- 多状态筛选（全部/待付款/待发货/待收货/已完成）
- 订单列表含状态标签
- 操作按钮：去支付/确认收货/评价
- 评价弹窗：星级评分 + 文字评价

**个人中心：**
- 用户信息展示
- 收货地址管理（新增/列表）
- 领券中心入口 → 弹窗展示可领优惠券 → 点击领取
- 我的优惠券卡片展示

### 4.4 后台功能设计

**商家后台：**

| 页面 | 功能 |
|------|------|
| 数据看板 | 渐变色统计卡片：商品总数/总订单/今日订单/营收 |
| 商品管理 | 商品列表 + 删除 + 跳转编辑 |
| 发布商品 | 表单：名称/品牌/描述/类目选择 + 多SKU动态添加 + 保存上架 |
| 订单管理 | 订单列表含商品明细 + 待发货订单可发货（录入快递信息） |
| 优惠券管理 | 优惠券列表 + 新建弹窗（名称/类型/门槛/金额/数量）+ 删除 |

**平台管理后台：**

| 页面 | 功能 |
|------|------|
| 数据大屏 | 渐变色卡片：注册用户/商品总数/订单总数/平台营收 |
| 用户管理 | 用户列表（手机号/昵称/状态/注册时间）+ 启用/禁用 |
| 商品审核 | 商品列表 + 状态标签 + 通过/驳回/下架操作 |
| 商家管理 | 预留界面 |
| 角色权限 | RBAC角色列表展示 |

---

## 5. 代码实现

### 5.1 开发架构

**架构演进路线（Git分支体现）：**

```
mv-architecture  →  mvc-architecture  →  vue-springboot  →  microservices
   MV两层             MVC三层            前后端分离          微服务+高并发
Thymeleaf渲染      +Service+JWT      Vue3+Redis+REST       (规划中)
```

**最终版本技术栈：**

| 层级 | 技术 |
|------|------|
| 前端框架 | Vue3 + TypeScript + Vite |
| UI组件库 | Element Plus |
| 状态管理 | Pinia |
| HTTP请求 | Axios（拦截器封装） |
| 后端框架 | Spring Boot 3.2 + MyBatis Plus 3.5 |
| 认证 | JWT + BCrypt |
| 缓存 | Redis + Redisson（分布式锁） |
| 数据库 | MySQL 8.0（20张表） |
| API文档 | Knife4j |

**项目结构（最终版）：**
```
CloudMall/
├── pom.xml                    # Maven父POM
├── init.sql                   # 数据库初始化（20张表+示例数据）
├── docker-compose.yml         # 中间件编排
├── src/main/java/com/cloudmall/
│   ├── controller/            # 7个Controller
│   │   ├── UserController.java
│   │   ├── ProductController.java / ProductApiController.java
│   │   ├── CartController.java
│   │   ├── OrderController.java
│   │   ├── MerchantController.java
│   │   ├── AdminController.java
│   │   ├── CouponController.java
│   │   ├── AddressController.java
│   │   └── ReviewController.java
│   ├── service/impl/          # 5个Service
│   │   ├── UserServiceImpl.java
│   │   ├── ProductServiceImpl.java
│   │   ├── CartServiceImpl.java (Redis Hash)
│   │   ├── OrderServiceImpl.java (Redisson锁)
│   │   └── AddressServiceImpl.java
│   ├── mapper/                # 9个Mapper接口
│   ├── entity/                # 12个实体类
│   └── common/                # 公共模块
│       ├── result/R.java      # 统一返回体
│       ├── exception/         # 全局异常处理
│       ├── config/            # Redis/JWT/Web配置
│       └── utils/JwtUtil.java
├── vue-frontend/
│   ├── src/
│   │   ├── views/portal/      # 8个C端页面
│   │   ├── views/merchant/    # 5个商家端页面
│   │   ├── views/admin/       # 6个平台端页面
│   │   ├── router/            # 三端路由
│   │   ├── stores/            # Pinia状态
│   │   ├── api/               # API封装
│   │   └── utils/request.ts   # Axios拦截器
│   ├── vite.config.ts
│   └── package.json
```

### 5.2 主要功能代码实现

**1. Redis购物车实现（CartServiceImpl.java）：**

购物车使用 Redis Hash 结构存储，Key 为 `cart:{userId}`，Field 为 `skuId`，Value 为商品详情 JSON。

```java
public void addItem(Long userId, Long skuId, String productName,
                    String specInfo, Integer price, Integer quantity, String image) {
    String key = "cart:" + userId;
    Object existing = redisTemplate.opsForHash().get(key, skuId.toString());
    if (existing != null) {
        // 已存在则数量累加
        Map<String, Object> item = (Map<String, Object>) existing;
        item.put("quantity", (Integer) item.get("quantity") + quantity);
        redisTemplate.opsForHash().put(key, skuId.toString(), item);
    } else {
        // 新增商品到购物车
        Map<String, Object> item = new HashMap<>();
        item.put("skuId", skuId);
        item.put("productName", productName);
        item.put("price", price);
        item.put("quantity", quantity);
        item.put("checked", true);
        redisTemplate.opsForHash().put(key, skuId.toString(), item);
    }
}
```

**2. 分布式锁防超卖（Redisson）：**

下单时使用 Redisson 分布式锁 + MySQL 乐观锁双重保障：

```java
RLock lock = redissonClient.getLock("stock:" + skuId);
try {
    if (lock.tryLock(3, 5, TimeUnit.SECONDS)) {
        Sku sku = skuMapper.selectById(skuId);
        if (sku == null || sku.getStock() < quantity) {
            throw new BusinessException("库存不足");
        }
        sku.setStock(sku.getStock() - quantity);
        skuMapper.updateById(sku);
    }
} finally {
    if (lock.isHeldByCurrentThread()) lock.unlock();
}
```

**3. 优惠券计算（支持满减券+折扣券）：**

```java
if (ct.getType() == 2) {
    // 折扣券: value=85 表示8.5折
    discountAmount = totalAmount * (100 - ct.getValue()) / 100;
} else {
    // 满减券: value=减免金额
    discountAmount = ct.getValue();
}
```

**4. 前端优惠券门槛校验：**

```typescript
const discount = computed(() => {
  const c = coupons.value.find(c => c.id === selectedCouponId.value)
  if (!c || total.value < c.threshold) return 0  // 未满门槛
  if (c.type === 1) return c.value  // 满减券
  if (c.type === 2) return Math.floor(total.value * (100 - c.value) / 100)  // 折扣券
  return 0
})
```

**5. JWT认证 + Axios拦截器：**

```typescript
request.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) config.headers.Authorization = `Bearer ${token}`
  return config
})

request.interceptors.response.use(res => {
  if (res.data.code !== 200) {
    ElMessage.error(res.data.message || '请求失败')
    return Promise.reject(res.data)
  }
  return res.data
})
```

---

## 6. 结束语

### 项目收获

通过本次课程设计，我完成了从 MV 两层架构到 MVC 三层架构，再到 Vue+SpringBoot 前后端分离架构的完整演进过程，对 Web 开发架构有了深入理解。

**技术收获：**

1. **架构演进理解：** 通过 Git 分支管理，清晰展示了 MV→MVC→前后端分离→微服务的架构演进路径，每一阶段都是对前一阶段不足的改进
2. **前端工程化：** 掌握了 Vue3 Composition API、Pinia 状态管理、Vue Router 路由、Element Plus 组件库的使用
3. **后端分层设计：** Controller-Service-Mapper 三层解耦，统一返回体 R\<T\>、全局异常处理
4. **高并发方案：** Redis 缓存策略、Redisson 分布式锁防超卖、优惠券门槛计算
5. **安全认证：** BCrypt 密码加密、JWT 无状态认证、拦截器统一鉴权
6. **数据库设计：** 三库设计为微服务拆分做准备，20张表涵盖电商全部业务

**解决的问题：**
- SKU 规格 JSON 存储与前端格式化显示
- 购物车 Redis Hash 结构的选型优势
- 下单时分布式锁 + 乐观锁防止超卖
- 满减券与折扣券的计算差异化处理
- 前后端分离下的 CORS 代理配置

### 不足与展望

1. 拼团、秒杀、预售等高级营销功能尚未实现
2. 真正的第三方支付/物流接口使用了模拟数据
3. 微服务拆分还停留在规划阶段
4. 搜索引擎可升级为 Elasticsearch 替代 MySQL LIKE

---

## 7. 软件运行配置

### 环境要求

- JDK 17+
- Maven 3.8+
- Node.js 18+
- MySQL 8.0
- Redis 7+

### 启动步骤

**1. 初始化数据库：**
```bash
mysql -u root -p < init.sql
```

**2. 配置连接信息：**
修改 `src/main/resources/application.yml` 中的 MySQL 和 Redis 连接地址。

**3. 启动后端：**
```bash
mvn spring-boot:run
# 后端运行在 http://localhost:8080
```

**4. 启动前端：**
```bash
cd vue-frontend
npm install
npx vite --host 0.0.0.0
# 前端运行在 http://localhost:3000
```

**5. 访问系统：**

| 端 | 地址 | 测试账号 |
|---|------|---------|
| 用户前台 | http://IP:3000/ | 13800001111 / 123456 |
| 商家后台 | http://IP:3000/merchant/dashboard | 同上 |
| 平台后台 | http://IP:3000/admin/dashboard | admin / admin123 |
| API文档 | http://IP:8080/doc.html | - |

### Git 分支说明

```
main              → 项目文档起点
mv-architecture   → 分支一：MV两层架构演示
mvc-architecture  → 分支二：MVC三层架构演示
vue-springboot    → 分支三：前后端分离全功能版（最终版本）
```

---

**报告完成日期：** 2026年6月25日

---

## 附录：源代码及数据库

### 源代码

项目源代码托管在 Git 仓库，四个分支展示架构演进：

| 分支 | 说明 | 代码行数 |
|------|------|---------|
| mv-architecture | MV两层架构 | 前端Thymeleaf模板 + 后端Controller/Mapper |
| mvc-architecture | MVC三层架构 | +Service层 + JWT + BCrypt + 统一返回 |
| vue-springboot | 前后端分离全功能版 | 后端7Controller + 5Service + 20表 + 前端16页面 |

### 数据库脚本

`init.sql` 文件包含 20 张表的完整建表语句及示例数据，位于项目根目录。

### 提交文件清单

1. 课程实践报告.md（本文件）
2. init.sql（数据库初始化脚本）
3. pom.xml（Maven项目配置）
4. src/（后端源代码）
5. vue-frontend/src/（前端源代码）
6. 需求分析报告.md
7. 实施计划.md
